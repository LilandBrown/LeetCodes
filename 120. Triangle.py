class Solution(object):
    def minimumTotal(self, triangle):
        """
        :type triangle: List[List[int]]
        :rtype: int
        INCOMPLETE BECAUSE BORED
        """
        for i in range(1, len[triangle]):
            for j in range[i]:
                triangle [i] [j] += min(triangle [i-1] [max(0,j-1)],  triangle [i-1] [max(j, len(triangle[i]) - )])
        maximum = triangles [len(triabgle)] 
        for i in triangle [len(triangle)]:
            if i > maximum
